---
const { titulo, fechaInicio, fechaFin, entidad } = Astro.props;
---

<div class="text-white border-b pb-8 flex flex-col gap-8 mt-12">
  <div class="flex flex-col sm:flex-row justify-between xl:items-center">
    <h2 class="text-lg xl:text-4xl font-semibold ease-in duration-300">
      {titulo}
    </h2>
    <p class="mt-4 sm:m-1">{fechaInicio} - {fechaFin}</p>
  </div>
  <p>{entidad}</p>
</div>
